# 终端日志规范

## 描述
终端日志功能规划提供一个类似控制台的界面，用于显示系统事件、命令执行和状态更新。它作为调试工具，为用户提供系统操作的透明度。

> 当前状态：本规范定义目标行为。当前版本**未实现**终端日志 UI 组件，也没有 `logService`/`terminalService`。`TerminalLogEntry` 类型仅作为数据模型在规范中描述，代码尚未落地。为未来接入真实命令执行时提供透明度基础。
## 需求
### 需求：日志结构
系统应定义终端日志条目的结构化格式。
#### 场景：日志创建
- **WHEN** 系统事件发生
- **THEN** 系统应创建带有唯一 ID、时间戳、消息和类型的日志条目
- **AND** 将其添加到终端日志
#### 场景：日志显示
- **WHEN** 添加日志条目
- **THEN** 系统应在终端组件中显示它
- **AND** 自动滚动到底部
### 需求：日志类型
系统应支持不同类型的日志条目以进行适当的可视化。
#### 场景：信息消息
- **WHEN** 发生信息性事件
- **THEN** 系统应创建类型为 "info" 的日志条目
- **AND** 以默认文本颜色显示它

#### 场景：成功消息
- **WHEN** 发生成功操作
- **THEN** 系统应创建类型为 "success" 的日志条目
- **AND** 以绿色文本显示它

#### 场景：警告消息
- **WHEN** 发生警告事件
- **THEN** 系统应创建类型为 "warning" 的日志条目
- **AND** 以黄色文本显示它

#### 场景：错误消息
- **WHEN** 发生错误事件
- **THEN** 系统应创建类型为 "error" 的日志条目
- **AND** 以红色文本显示它

#### 场景：命令消息
- **WHEN** 执行命令
- **THEN** 系统应创建类型为 "command" 的日志条目
- **AND** 以蓝色文本显示它，并带有 $ 前缀

### 需求：增强终端功能
系统应提供增强的终端功能，以提高用户体验和调试效率。
#### 场景：日志过滤
- **WHEN** 用户应用过滤器
- **THEN** 系统应仅显示符合过滤条件的日志条目
- **AND** 相应更新显示

#### 场景：日志导出
- **WHEN** 用户请求导出日志
- **THEN** 系统应生成包含所有日志条目的文本文件
- **AND** 提示用户保存位置
#### 场景：日志搜索
- **WHEN** 用户输入搜索词
- **THEN** 系统应仅显示包含搜索词的日志条目
- **AND** 允许在搜索结果之间导航

#### 场景：日志级别过滤
- **WHEN** 用户调整日志级别设置
- **THEN** 系统应仅显示等于或高于指定级别的日志条目
- **AND** 相应更新显示

#### 场景：日志持久化
- **WHEN** 发生重要事件
- **THEN** 系统应将关键日志保存到本地文件系统
- **AND** 允许用户稍后访问它们

## 数据结构

### TerminalLogEntry
```typescript
interface TerminalLogEntry {
  id: string;
  timestamp: string;
  message: string;
  type: 'info' | 'success' | 'warning' | 'error' | 'command';
}
```

## 实现注意事项

### 实现状态
- 当前未实现终端日志 UI 与 `logService`；本规范为未来接入真实命令执行时预留
- 若未来采用 Tauri 提供原生能力，可借助其文件系统 API 进行日志持久化，但当前未确定

### 性能优化
- 实现日志条目的虚拟滚动，以处理大量日志
- 考虑限制内存中存储的日志数量，实现滚动缓冲区
- 对频繁的日志更新使用批处理，以提高性能

### 用户体验
- 提供日志清空调度，允许用户重置终端
- 实现日志复制功能，以便用户可以复制特定消息
- 考虑添加时间戳格式选项，以适应不同的用户偏好